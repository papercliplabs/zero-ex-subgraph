{
    "name": "0x-protocol-mainnet",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build:template": "graph build --network=$NETWORK --output-dir=build/$NAME --skip-migrations",
        "deploy-hosted:template": "source .env; graph deploy --node https://api.thegraph.com/deploy/ papercliplabs/0x-protocol-$NAME --network=$NETWORK --deploy-key $HOSTED_ACCESS_TOKEN --output-dir=build/$NAME --skip-migrations",
        "deploy-studio:template": "source .env; graph deploy --studio 0x-protocol-$NAME --deploy-key $STUDIO_DEPLOY_KEY --network=$NETWORK --version-label $STUDIO_VERSION --output-dir=build/$NAME",
        "deploy-alchemy:template": "source .env; graph deploy --node https://subgraphs.alchemy.com/api/subgraphs/deploy 0x-protocol-$NAME --ipfs https://ipfs.satsuma.xyz --network=$NETWORK --version-label $STUDIO_VERSION --deploy-key $ALCHEMY_DEPLOY_KEY --output-dir=build/$NAME --skip-migrations",
        "auto-deploy:template": "yarn codegen; yarn build:$NAME; yarn deploy-hosted:$NAME; yarn deploy-studio:$NAME; yarn deploy-alchemy:$NAME;",
        "build:mainnet": "export NETWORK=mainnet; export NAME=mainnet; yarn build:template",
        "deploy-hosted:mainnet": "export NETWORK=mainnet; export NAME=mainnet; yarn deploy-hosted:template",
        "deploy-studio:mainnet": "export NETWORK=mainnet; export NAME=mainnet; yarn deploy-studio:template",
        "deploy-alchemy:mainnet": "export NETWORK=mainnet; export NAME=mainnet; yarn deploy-alchemy:template",
        "auto-deploy:mainnet": "export NETWORK=mainnet; export NAME=mainnet; yarn auto-deploy:template",
        "build:optimism": "export NETWORK=optimism; export NAME=optimism; yarn build:template",
        "deploy-hosted:optimism": "export NETWORK=optimism;mexport NAME=optimism; yarn deploy-hosted:template",
        "deploy-studio:optimism": "export NETWORK=optimism; export NAME=optimism; yarn deploy-studio:template",
        "deploy-alchemy:optimism": "export NETWORK=optimism; export NAME=optimism; yarn deploy-alchemy:template",
        "auto-deploy:optimism": "export NETWORK=optimism; export NAME=optimism; yarn auto-deploy:template",
        "build:arbitrum": "export NETWORK=arbitrum-one; export NAME=arbitrum; yarn build:template",
        "deploy-hosted:arbitrum": "export NETWORK=arbitrum-one; export NAME=arbitrum; yarn deploy-hosted:template",
        "deploy-studio:arbitrum": "export NETWORK=arbitrum-one; export NAME=arbitrum; yarn deploy-studio:template",
        "deploy-alchemy:arbitrum": "export NETWORK=arbitrum-one; export NAME=arbitrum; yarn deploy-alchemy:template",
        "auto-deploy:arbitrum": "export NETWORK=arbitrum-one; export NAME=arbitrum; yarn auto-deploy:template",
        "build:base": "export NETWORK=base; export NAME=base; yarn build:template",
        "deploy-hosted:base": "export NETWORK=base; export NAME=base; yarn deploy-hosted:template",
        "deploy-studio:base": "export NETWORK=base; export NAME=base; yarn deploy-studio:template",
        "deploy-alchemy:base": "export NETWORK=base; export NAME=base; yarn deploy-alchemy:template",
        "auto-deploy:base": "export NETWORK=base; export NAME=base; yarn auto-deploy:template",
        "build:polygon": "export NETWORK=matic; export NAME=polygon; yarn build:template",
        "deploy-hosted:polygon": "export NETWORK=matic; export NAME=polygon; yarn deploy-hosted:template",
        "deploy-studio:polygon": "export NETWORK=matic; export NAME=polygon; yarn deploy-studio:template",
        "deploy-alchemy:polygon": "export NETWORK=matic; export NAME=polygon; yarn deploy-alchemy:template",
        "auto-deploy:polygon": "export NETWORK=matic; export NAME=polygon; yarn auto-deploy:template",
        "build:bnb": "export NETWORK=bsc; export NAME=bnb; yarn build:template",
        "deploy-hosted:bnb": "export NETWORK=bsc; export NAME=bnb; yarn deploy-hosted:template",
        "deploy-studio:bnb": "export NETWORK=bsc; export NAME=bnb; yarn deploy-studio:template",
        "deploy-alchemy:bnb": "export NETWORK=bsc; export NAME=bnb; yarn deploy-alchemy:template",
        "auto-deploy:bnb": "export NETWORK=bsc; export NAME=bnb; yarn deploy-hosted:bnb",
        "build:avalanche": "export NETWORK=avalanche; export NAME=avalanche; yarn build:template",
        "deploy-hosted:avalanche": "export NETWORK=avalanche; export NAME=avalanche; yarn deploy-hosted:template",
        "deploy-studio:avalanche": "export NETWORK=avalanche; export NAME=avalanche; yarn deploy-studio:template",
        "auto-deploy:avalanche": "export NETWORK=avalanche; export NAME=avalanche; yarn auto-deploy:template",
        "deploy-alchemy:avalanche": "export NETWORK=avalanche; export NAME=avalanche; yarn deploy-alchemy:template",
        "build:fantom": "export NETWORK=fantom; export NAME=fantom; yarn build:template",
        "deploy-hosted:fantom": "export NETWORK=fantom; export NAME=fantom; yarn deploy-hosted:template",
        "deploy-studio:fantom": "export NETWORK=fantom; export NAME=fantom; yarn deploy-studio:template",
        "deploy-alchemy:fantom": "export NETWORK=fantom; export NAME=fantom; yarn deploy-alchemy:template",
        "auto-deploy:fantom": "export NETWORK=fantom; export NAME=fantom; yarn auto-deploy:template",
        "build:celo": "export NETWORK=celo; export NAME=celo; yarn build:template",
        "deploy-hosted:celo": "export NETWORK=celo; export NAME=celo; yarn deploy-hosted:template",
        "deploy-studio:celo": "export NETWORK=celo; export NAME=celo; yarn deploy-studio:template",
        "auto-deploy:celo": "export NETWORK=celo; export NAME=celo; yarn auto-deploy:template",
        "deploy-alchemy:celo": "export NETWORK=celo; export NAME=celo; yarn deploy-alchemy:template",
        "auto-deploy:all": "yarn auto-deploy:mainnet; yarn auto-deploy:optimism; yarn auto-deploy:arbitrum; yarn auto-deploy:base; yarn auto-deploy:polygon; yarn auto-deploy:bnb; yarn auto-deploy:avalanche; yarn auto-deploy:fantom; yarn auto-deploy:celo",
        "create-local": "source .env; graph create --node http://${LOCAL_NODE_IP}:8020/ papercliplabs/0x-protocol-mainnet-final-optimized",
        "remove-local": "source .env; graph remove --node http://${LOCAL_NODE_IP}:8020/ papercliplabs/0x-protocol-mainnet-final-optimized",
        "deploy-local": "source .env; graph deploy --node http://${LOCAL_NODE_IP}:8020/ --ipfs http://${LOCAL_NODE_IP}:5001 papercliplabs/0x-protocol-mainnet-final-optimized --output-dir=build/mainnet --version-label v0",
        "test": "graph test"
    },
    "dependencies": {
        "@babel/core": "^7.23.9",
        "@babel/preset-typescript": "^7.23.3",
        "@graphprotocol/graph-cli": "^0.67.2",
        "@graphprotocol/graph-ts": "0.30.0"
    },
    "devDependencies": {
        "matchstick-as": "0.5.0"
    }
}
